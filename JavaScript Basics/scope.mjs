// {} - Blocks

// {
//     let a = 1;
//     console.log(a);
// }


// console.log(a);

// A variable will be known within its block from the line where it is defined



/* {
    // Parent Block
    let a = 2; {
        // child block
        console.log(a);
    }
    // console.log(a);
}
 */
// When a variable is called , first it is searched in its own block, if the variable is not find in that block then it will be searched in its parent block

// A variable can not be redefined ( defined twice ) in same block, but we can redefined same variable in different block